# Dependencies
node_modules/

# Build
dist/
.next/
out/

# Turbo
.turbo/

# Prisma
apps/api/prisma/generated/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test
coverage/

# Logs
*.log

# Debug
firebase-debug.log

# Secrets / service accounts — catch-all so nothing slips through
firebase-service-account.json
*firebase*sa*.json
*firebase*service-account*.json
*-sa.json
*service-account*.json

# Screenshots (dev testing) — root level only, not public assets
/*.png

# Playwright MCP
.playwright-mcp/

# Claude memory (local only)
.claude/

# Podcast video assets — committed once, shipped via Railway image bake.
# If updating an audiogram, re-commit the file (note: old blob stays in history).
# apps/web/public/clips/*.mp4   # ← keep this commented; un-comment if you want to LFS-ify later
